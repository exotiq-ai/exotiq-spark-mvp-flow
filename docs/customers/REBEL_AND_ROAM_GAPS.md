# Rebel & Roam (Ed) — Q&A gaps to close

Raised 2026-08-11 after Ed loaded his full fleet.

| # | Ask | Today | Gap / next update |
|---|-----|-------|-------------------|
| 1 | Log previous bookings | Manual: create booking with past dates, then mark returned. Bookings CSV import exists in the import wizard but is not surfaced outside Fleet/Onboarding. | Surface "Import bookings" from the Bookings module; add a "historical booking" quick-entry that skips inspections/payment steps. |
| 2 | Per-mile overage rate | Editable per vehicle (Included Miles/Day + Overage Rate) in Add/Edit Vehicle. | No fleet-wide bulk edit and no team default. Add bulk apply + tenant default in Settings. |
| 3 | Multi-day pricing | Rate Tiers panel in MotorIQ — per vehicle, per duration tier. | No bulk/percentage-based tier setup (e.g. "weekly = 15% off daily"). Add bulk tier rules. |
| 4 | Connect Stripe | Settings → Payments → Connect Stripe Account (Express). Live. | None. |
| 5 | Investor dashboard | **Not available.** Per-vehicle P&L exists but only for full-access roles; Viewer role sees the whole command center. | Build read-only investor view scoped to specific vehicles + shareable link. Flagged for roadmap. |
| 6 | Bulk customer upload | Supported — import wizard has a `customers` entity type. | Entry point is buried in Fleet; expose under Customers module. |
